
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneyordeath.app',
  appName: 'MoneyOrDeath',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
